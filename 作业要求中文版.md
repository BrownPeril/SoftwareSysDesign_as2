# 软件体系结构 Assignment 2: Attribute Driven Design (ADD) — 作业要求

## 一、作业概述

本作业要求使用 **ADD 3.0（Attribute-Driven Design）方法**，通过 **Direct LLM Interaction** 范式，让 LLM（gemini-3.1-pro-preview）充当软件架构师，为 **Hotel Price System (HPS)** 设计软件架构。

整个设计过程分为 **4 次迭代**，每次迭代遵循 ADD 的 Step 2 → Step 7，逐步完善架构设计。

---

## 二、迭代计划

| 迭代 | 目标 | 执行者 |
|------|------|--------|
| 迭代 1 | 建立整体系统结构 | 成员 A |
| 迭代 2 | 识别支持主要功能的结构 | 成员 B |
| 迭代 3 | 处理可靠性与可用性质量属性 | 成员 B |
| 迭代 4 | 处理开发与运维 | 成员 C |

> **顺序约束**：迭代 1 → 迭代 2 → 迭代 3 → 迭代 4，后一次必须使用前一次的输出作为输入。

---

## 三、ADD 3.0 方法的 7 个步骤

### Step 1（全局仅做一次）
> Review Inputs: identifying which requirements will be considered as architectural drivers.

- 列出所有输入：HPS 的 6 个用例、9 个质量属性、5 个关注点、6 个约束
- 说明这是一个成熟领域的绿地系统

### 每次迭代中的 Step 2 ~ Step 7

| 步骤 | 名称 | 内容要求 |
|------|------|----------|
| **Step 2** | Establish the iteration goal by selecting drivers | 明确写出本次迭代的目标，选择哪些驱动（用例/质量属性/关注点/约束）作为本次要满足的子集 |
| **Step 3** | Choose one or more elements of the system to refine | 迭代 1 选择"系统本身"；后续迭代选择前次迭代中产生的具体元素 |
| **Step 4** | Choose one or more design concepts that satisfy the selected drivers | 列出至少两个候选设计概念，比较优缺点，选择一个。理由必须来自先验知识 |
| **Step 5** | Instantiate architectural elements, allocate responsibilities, define interfaces | 用**表格**给出：元素名称、职责、属性。定义接口（REST API 端点、消息主题等） |
| **Step 6** | Sketch views and record design decisions | 使用 **Mermaid** 代码块绘制视图。迭代 1：模块视图；迭代 2：C&C 视图；迭代 3：部署视图；迭代 4：开发视图 |
| **Step 7** | Perform analysis of current design and review iteration goal | 分析是否满足所选驱动，说明是否达到迭代目标 |

---

## 四、HPS（Hotel Price System）需求

### 4.1 设计目的

绿地系统（Greenfield），完全替换现有系统。

### 4.2 六个用例（HPS-1 ~ HPS-6）

| 编号 | 用例名称 | 简要描述 |
|------|----------|----------|
| HPS-1 | Manage Hotel Information | 管理酒店信息（增删改查） |
| HPS-2 | Change Prices | 修改价格（批量/单个） |
| HPS-3 | Query Prices | 查询价格（按酒店、日期等条件） |
| HPS-4 | Manage Hotels | 管理酒店（CRUD 操作） |
| HPS-5 | Synchronize with External CMS | 与外部 CMS 同步数据 |
| HPS-6 | User Login & Authentication | 用户登录与认证 |

### 4.3 九个质量属性（QA-1 ~ QA-9）

| 编号 | 质量属性 | 场景描述 | 关联用例 | 重要度 |
|------|----------|----------|----------|--------|
| QA-1 | 性能 | 价格查询响应时间 < 100ms（95 分位） | HPS-3 | 高 |
| QA-2 | 可靠性 | 价格变更发布成功率 100%（最多重试 3 次） | HPS-2 | 高 |
| QA-3 | 可用性 | 查询服务 uptime ≥ 99.9% | HPS-3 | 高 |
| QA-4 | 安全性 | 仅认证用户可修改价格 | HPS-2, HPS-6 | 高 |
| QA-5 | 可修改性 | 添加新价格策略的代码修改影响 < 5 个文件 | HPS-2 | 中 |
| QA-6 | 可测试性 | 价格计算逻辑可在无数据库依赖下单元测试 | HPS-2 | 中 |
| QA-7 | 可伸缩性 | 支持并发价格查询量增长 10 倍 | HPS-3 | 中 |
| QA-8 | 互操作性 | 与外部 CMS 的数据格式兼容 | HPS-5 | 中 |
| QA-9 | 可部署性 | 新版本部署无需停机 | HPS-3 | 低 |

### 4.4 五个架构关注点（CRN-1 ~ CRN-5）

| 编号 | 关注点 |
|------|--------|
| CRN-1 | 系统需支持多渠道分发（Web、Mobile、API） |
| CRN-2 | 团队熟悉 Java/Angular/Kafka 技术栈 |
| CRN-3 | 需要支持团队并行工作分配 |
| CRN-4 | 系统需提供审计日志 |
| CRN-5 | 需要持续部署基础设施 |

### 4.5 六个约束（CON-1 ~ CON-6）

| 编号 | 约束 |
|------|------|
| CON-1 | 必须部署在云基础设施上 |
| CON-2 | 必须使用关系型数据库（PostgreSQL） |
| CON-3 | 必须使用 Git 平台进行版本控制 |
| CON-4 | 6 个月交付，MVP 需在 2 个月内完成 |
| CON-5 | 必须支持与现有外部 CMS 的集成 |
| CON-6 | 前端采用 Angular 框架 |

---

## 五、Key Points（作业关键要求）

1. **Prior Knowledge**：所有先验知识（ADD 3.0 定义 + HPS 需求）必须完整粘贴到每条 system prompt 中
2. **Role Prompt**：LLM 扮演严格遵守 ADD 3.0 方法的软件架构师
3. **禁止外部领域知识**：不能引入先验知识之外的任何知识
4. **禁止 Few-shot**：不能提供示例输出
5. **禁止任务重解释**：不能改变迭代目标
6. **决策必须显式引用**：例如"根据 QA-1 的性能要求（<100ms）"
7. **视图必须用 Mermaid / PlantUML**：本作业要求使用 Mermaid

---

## 六、交付物与评分标准（总分 50 分）

| 交付物 | 分值 | 要求 |
|--------|------|------|
| 1. 源代码 | 15 分 | Python 调用脚本（含自动时间戳日志），即 `.py` 或 `.ipynb` 文件 |
| 2. 完整对话日志 | 15 分 | 4 次迭代的完整日志（含 system, user, assistant 内容），带时间戳 |
| 3. 报告 | 20 分 | 按附录模板撰写，不超过 30 页 |

### 6.1 源代码要求

- 能够向 `https://api.ppio.com/openai` 发送请求
- 模型为 `pa/gemini-3.1-pro-preview`
- 每条请求包含：system prompt（先验知识 + 角色提示）和 user prompt（本次迭代指令）
- 自动在日志中记录时间戳（精确到秒）
- 保存每次迭代的完整对话到独立文件

### 6.2 日志要求

- 每个日志文件包含：
  - 请求开始的时间戳（格式：`YYYY-MM-DD HH:MM:SS`）
  - 完整的 System Prompt
  - 完整的 User Prompt
  - 完整的 Assistant Response
- 文件命名：`log_iter1.txt`, `log_iter2.txt`, `log_iter3.txt`, `log_iter4.txt`

### 6.3 报告模板结构

1. **封面**：课程名、作业名、小组成员、日期、所选方式
2. **ADD Step 1（全局输入回顾）**：列表形式展示所有输入
3. **迭代 1 输出**：Step 2~7 + Mermaid 图
4. **迭代 2 输出**：同上
5. **迭代 3 输出**：同上
6. **迭代 4 输出**：同上
7. **交互成本分析表**：

| The way of completing the assignment | The LLM used | Number of Human Interactions (turns) | Token Consumption (K tokens) |
|--------------------------------------|--------------|-------------------------------------|------------------------------|
| Direct LLM interaction | gemini-3.1-pro-preview | 4 | （根据 usage 累加） |

8. **Individual Reflection**（每人一段）
9. **贡献表**

---

## 七、各迭代的 Mermaid 视图要求

| 迭代 | 视图类型 | 示例内容 |
|------|----------|----------|
| 迭代 1 | 模块分解图 | 前端模块、后端服务模块、数据库模块、外部 CMS 模块 |
| 迭代 2 | C&C 图 | Price Change Controller, Query API, Channel Management System 适配器等 |
| 迭代 3 | 部署图 | 云节点、负载均衡器、应用实例主备、数据库主从 |
| 迭代 4 | 开发视图 | CI/CD 流水线、模块与团队映射 |

---

## 八、重要提醒

1. 禁止外部知识：不能在提示词中说"你应该使用微服务因为 Netflix 这么做"，必须引用 HPS 中的 QA 或 CON
2. 禁止 few-shot：不能给模型提供例子输出
3. 禁止任务重解释：不能改变迭代目标
4. 决策必须显式引用：例如"根据 QA-1 的性能要求（<100ms），我选择事件驱动架构"
5. 日志必须有时间戳
6. 源代码只需提交 `.py` 或 `.ipynb` 文件
7. 报告为英文（允许少量语法错误）