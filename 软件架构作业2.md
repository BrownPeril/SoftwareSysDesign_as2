# 软件架构作业2

本作业的主要目标是评估不同人工智能范式在支持软件架构设计方面的有效性。在本作业中，你需要使用 **ADD 3.0 方法**为给定的案例研究（酒店定价系统）进行架构设计。你将需要以三人一组的形式完成。

我们小组选择一种 AI 范式：直接 LLM 交互。然后，将 Gemini3.1pro preview（我们组选的模型）与所选 AI 范式结合，以执行 ADD 3.0 流程并生成架构设计。

## 1 作业详细说明与交付物

### 1.1 直接 LLM 交互（隐式单步推理）：

直接使用 LLM 进行交互，并使用 ADD 3.0 完成案例的架构设计。

**必须提供给 AI 的先验知识：**

- ADD 3.0（见第二部分“共享内容”）
- 案例研究 – 酒店定价系统（见第二部分“共享内容”）

**迭代计划：**

- 迭代 1：建立整体系统结构
- 迭代 2：识别支持主要功能的结构
- 迭代 3：处理可靠性和可用性质量属性
- 迭代 4：处理开发与运维

**要求：**

1. 迭代中产生的视图应使用 Mermaid 或 PlantUML 代码生成。
2. 不允许使用超出所提供先验知识的外部领域知识。
3. 提示中不允许使用少样本示例或手工制作的演示输出。
4. 不允许超出统一先验知识范围进行额外的任务重解释或需求增强。
5. 所有决策规则必须明确源自提供的系统指令；不允许使用隐式或外部规则库。

**编写系统指令的要点：**

1. 先验知识
2. 角色提示

**交付物包括：**

1. 源代码（15 分）
2. 与 LLM 交互的完整对话日志（含时间戳），共四次迭代（15 分）
3. 报告（见附录）（20 分）

## 第二部分：共享内容

### 2.1 属性驱动设计（ADD）方法：

#### 步骤 1 审查输入：

ADD 方法的第一步是审查输入，并确定哪些需求将被视为架构驱动因素。

#### 步骤 2 通过选择驱动因素建立迭代目标：

设计轮次通常采取一系列设计迭代的形式，每个迭代专注于实现特定目标。该目标通常涉及设计以满足一部分驱动因素。

#### 步骤 3 选择一个或多个待细化的系统元素：

这一步是核心设计活动的开始。你将选择的元素是那些参与满足特定驱动因素的元素。对于绿地开发，你可以从建立系统上下文开始，然后选择唯一可用的元素（即系统本身）进行分解细化。对于现有系统或绿地系统的后续设计迭代，你通常会选择细化先前迭代中已识别的元素。

#### 步骤 4 选择一个或多个满足所选驱动因素的设计概念：

此步骤要求你识别可用于实现迭代目标的设计概念中的备选方案，并选择其中一个。

#### 步骤 5 实例化架构元素、分配职责并定义接口：

此步骤要求基于选定的设计概念实例化架构元素，并分配

### 2.2 使用 ADD 设计成熟领域的绿地系统：酒店定价系统

#### 设计目的：

本项目可视为绿地开发，因为它涉及完全替换现有系统。设计活动的目的是做出初始决策，以支持从零开始构建系统。

#### 主要功能：

**[用例描述]**

| 用例 ID         | 描述                                                         |
| :-------------- | :----------------------------------------------------------- |
| HPS-1：登录     | 用户（商业用户或管理员）在登录窗口提供凭据。系统根据用户身份服务验证这些凭据，如果成功，则授予系统访问权限。登录后，用户只能查询和更改他们已获授权的酒店。 |
| HPS-2：更改价格 | 用户选择其有权更改价格的特定酒店，并选择日期以更改基本价或固定价。此时将计算所有根据基本价计算出的价格。系统允许在实际更改价格之前进行价格更改模拟。当价格更改后，它们会被推送到渠道管理系统，并可供外部系统查询。 |
| HPS-3：查询价格 | 用户或外部系统通过用户界面或查询 API 查询指定酒店的价格。    |
| HPS-4：管理酒店 | 管理员添加、更改或修改酒店信息。包括编辑酒店的税率、可用价格和房型。 |
| HPS-5：管理价格 | 管理员添加、更改或修改价格。包括定义不同价格的计算业务规则。 |
| HPS-6：管理用户 | 管理员更改指定用户的权限。                                   |

#### 质量属性：

| ID   | 质量属性 | 场景 [关联用例]                                              | 对客户的重要性 | 实现难度 |
| :--- | :------- | :----------------------------------------------------------- | :------------- | :------- |
| Q-1  | 性能     | 在正常操作期间更改了特定酒店和日期的基本价；酒店所有价格和房型的价格在 100 毫秒内发布（准备好查询）。[HPS-2] | 高             | 高       |
| Q-2  | 可靠性   | 用户对指定酒店进行多次价格更改；100% 的价格更改成功发布（可供查询）并被渠道管理系统接收。[HPS-2] | 高             | 高       |
| Q-3  | 可用性   | 价格查询的 SLA 在维护窗口之外必须达到 99.9%。[所有]          | 高             | 高       |
| Q-4  | 可扩展性 | 系统最初通过其 API 每天至少支持 100,000 次价格查询，并应能够处理高达 1,000,000 次，同时平均延迟增加不超过 20%。[HPS-3] | 高             | 高       |
| Q-5  | 安全性   | 用户通过前端登录系统。用户的凭据通过用户身份服务进行验证，登录后，他们只能看到其被授权使用的功能。[所有] | 高             | 中       |
| Q-6  | 可修改性 | 向系统中添加一个使用非 REST 协议（例如 gRPC）的价格查询端点。新端点不需要对系统的核心组件进行更改。[所有] | 中             | 中       |
| Q-7  | 可部署性 | 应用程序作为开发过程的一部分在非生产环境之间迁移。不需要更改代码。[所有] | 中             | 中       |
| Q-8  | 可监控性 | 系统操作员希望在运行期间测量价格发布的性能和可靠性。系统提供一种机制，允许根据需要收集 100% 的这些度量。[HPS-2] | 中             | 中       |
| Q-9  | 可测试性 | 系统和其元素的 100% 应支持独立于外部系统的集成测试。[所有]   | 中             | 中       |

#### 架构关注点：

| ID    | 关注点                                                  |
| :---- | :------------------------------------------------------ |
| CRN-1 | 建立整体初始系统结构。                                  |
| CRN-2 | 利用团队在 Java 技术、Angular 框架和 Kafka 方面的知识。 |
| CRN-3 | 将工作分配给开发团队成员。                              |
| CRN-4 | 避免引入技术债务。                                      |
| CRN-5 | 建立持续部署基础设施。                                  |

#### 约束：

| ID    | 约束                                                         |
| :---- | :----------------------------------------------------------- |
| CON-1 | 用户必须通过不同平台（Windows、OSX、Linux）和不同设备上的 Web 浏览器与系统交互。 |
| CON-2 | 通过云提供商身份服务管理用户，并将资源托管在云端。           |
| CON-3 | 代码必须托管在专有的基于 Git 的平台上，该平台已被公司其他项目使用。 |
| CON-4 | 系统的初始版本必须在 6 个月内交付，但系统的初始版本（MVP）必须在最多 2 个月内向内部利益相关者展示。 |
| CON-5 | 系统最初必须通过 REST API 与现有系统交互，但可能需要后续支持其他协议。 |
| CON-6 | 设计系统时应优先采用云原生方法。                             |

------

1. 每个步骤的输出结果（迭代 1：建立整体系统结构）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

2. 每个步骤的输出结果（迭代 2：识别支持主要功能的结构）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

3. 每个步骤的输出结果（迭代 3：处理可靠性和可用性质量属性）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

4. 每个步骤的输出结果（迭代 4：处理开发与运维）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

## Appendix: Report Template

（你**必须用英文编写并提交**报告。本作业没有最低长度要求，但报告不应超过三十页（A4 大小）。简洁明了。）

### 一、ADD 输出结果

**ADD 步骤 1：**

1. 每个步骤的输出结果（迭代 1：建立整体系统结构）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

2. 每个步骤的输出结果（迭代 2：识别支持主要功能的结构）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

3. 每个步骤的输出结果（迭代 3：处理可靠性和可用性质量属性）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

4. 每个步骤的输出结果（迭代 4：处理开发与运维）

- ADD 步骤 2：
- ADD 步骤 3：
- ADD 步骤 4：
- ADD 步骤 5：
- ADD 步骤 6：
- ADD 步骤 7：

### 二、交互成本分析

| 作业完成方式 | 使用的 LLM | 人工交互次数 | 令牌消耗（K tokens） |
| :----------- | :--------- | :----------- | :------------------- |
|              |            |              |                      |

### 三、个人反思

1. 遇到的问题及采用的解决方案
2. 个人对小组工作的贡献详述

| 姓名（中文） | 贡献 |
| :----------- | :--- |
|              |      |